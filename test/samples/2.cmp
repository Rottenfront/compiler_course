func fib (n : int) : int =
    if n <= 1 then
        1
    else
        fib (n - 1) + fib (n - 2)


func fib2 (n : int) (a : int) (b : int) : int = if n <= 1 then b else fib2 (n - 1) b (a + b)

func main : () =
    let x = read in
    print_int x;
    print_int (fib x);
    print_int (fib2 x 1 1)
